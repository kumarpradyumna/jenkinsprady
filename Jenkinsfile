pipeline {
    agent any 

    options {
        timestamps()
        overrideIndexTriggers(true)
    }
    stages {
        stage ('build') {
            steps{
                echo "Hello world 1"
            }
            
        }
    }
}
